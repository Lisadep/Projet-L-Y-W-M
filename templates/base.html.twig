<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {# Bootstrap 5.1.3 #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
    {# Font Awesome 5.13.1 (pour les îcones) #}
    <script src="https://kit.fontawesome.com/45e38e596f.js" crossorigin="anonymous"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.13.1/css/all.min.css">
    {# Notre style.css #}
    <link rel="stylesheet" href="{{ asset('styles/style.css') }}">
    {# Animate pour les animations #}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>
    {# Icône page navigateur #}
    <link rel="shortcut icon" type="image/x-icon" href="{{ asset ('images/favicon.png') }}" />
</head>

<title>Luxury Travel</title>

{# Header #}

<body>
    <div class="container py-3">
        <header>
            <div class="d-flex flex-column flex-md-row align-items-center pb-3 mb-4 border-bottom">
                <a href="/" class="d-flex align-items-center text-dark text-decoration-none">
                    <img class="logo" src="{{ asset('images/logo.png') }}" alt="logo">
                </a>

                <nav class="d-inline-flex m-2 mt-md-0 ms-md-auto">
                    <a class="me-3 py-2 text-dark text-decoration-none"
                        href="{{ path ('app_contact') }}"><i class="bi bi-envelope"></i> Contactez-nous</a>
                    {# S'il y a une session ouverte #}
                    {% if app.user %}
                    <a class="me-3 py-2 text-primary text-decoration-none" href="{{ path ('app_panier') }}"><i class="bi bi-cart"></i> Panier</a>
                    <a class="me-3 py-2 text-primary text-decoration-none" href="{{ path ('app_logout') }}"><i class="bi bi-door-closed"></i> Déconnexion</a>
                    {% else %}
                    <a class="me-3 py-2 text-primary text-decoration-none" href="{{ path ('app_login') }}"><i class="bi bi-person-fill"></i> Connexion</a>
                    <a class="py-2 text-primary text-decoration-none" href="{{ path ('app_register') }}"><i class="bi bi-person-plus-fill"></i> S'inscrire</a>
                    {% endif %}
                    {% if app.user == true %}
                    {% for role in app.user.roles %}
                    {% if role == "ROLE_ADMIN" %}
                    <a class="py-2 text-danger text-decoration-none" href="{{ path ('admin') }}"><i class="bi bi-layout-text-window-reverse"></i> Back Office</a>
                    {% endif %}
                    {% endfor %}
                    {% endif %}
            </div>
            </nav>
    </div>
    </header>

    {% block body %} {# Page à afficher #} {% endblock %}

    {# Footer #}
    <footer class="pt-4 my-md-5 pt-md-5 border-top">
        <div class="row">
            <div class="col-12 col-md icones">
                <small class="d-block mb-3 text-muted">&copy; 2022 Luxury Travel<br> Inc. Tous Droits Réservés</small>
                    <i class="iconecont bi bi-facebook"></i>
                    <i class="iconecont bi bi-linkedin"></i>
                    <i class="iconecont bi bi-twitter"></i>
            </div>
            <div class="col-6 col-md">
                <h5>Contact</h5>
                <ul class="list-unstyled text-small">
                    <li class="mb-1"><a class="link-secondary text-decoration-none"
                            href="{{ path ('app_contact') }}">Recrutement</a></li>
                    <li class="mb-1"><a class="link-secondary text-decoration-none"
                            href="{{ path ('app_contact') }}">Staff</a></li>
                    <li class="mb-1"><a class="link-secondary text-decoration-none"
                            href="{{ path ('app_contact') }}">Contactez-nous</a></li>
                </ul>
            </div>
            <div class="col-6 col-md">
                <h5>Services</h5>
                <ul class="list-unstyled text-small">
                    <li class="mb-1"><a class="link-secondary text-decoration-none" href="#">Service client</a></li>
                    <li class="mb-1"><a class="link-secondary text-decoration-none" href="#">S.A.V</a></li>
                </ul>
            </div>
            <div class="col-6 col-md">
                <h5>A propos de nous</h5>
                <ul class="list-unstyled text-small">
                    <li class="mb-1"><a class="link-secondary text-decoration-none" href="#">Team</a></li>
                    <li class="mb-1"><a class="link-secondary text-decoration-none" href="#">Location</a></li>
                    <li class="mb-1"><a class="link-secondary text-decoration-none" href="#">Conditions de vente</a>
                    </li>
                </ul>
            </div>
        </div>
    </footer>

    {% block javascripts %}
    {# Bootstrap #}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous">
    </script>
    {# Jquery #}
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    {# Notre JS #}
    <script src="{{ asset('js/script.js') }}" type="text/javascript"></script>
    {% endblock %}

</html>